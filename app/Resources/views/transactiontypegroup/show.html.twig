{% extends 'base.html.twig' %}

{% block body %}
    <h1>TransactionTypeGroup</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transactionTypeGroup.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ transactionTypeGroup.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ transactionTypeGroup.description }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{% if transactionTypeGroup.createdAt %}{{ transactionTypeGroup.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Updatedat</th>
                <td>{% if transactionTypeGroup.updatedAt %}{{ transactionTypeGroup.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('transaction_type_group_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('transaction_type_group_edit', { 'id': transactionTypeGroup.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
